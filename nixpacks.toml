[phases.setup]
nixPkgs = ["python312", "pip"]

[phases.install]
cmds = ["python3 -m pip install --upgrade pip && python3 -m pip install -e ."]

[start]
cmd = "python3 -m uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8000}"
